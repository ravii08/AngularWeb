import { FileterDataPipe } from './filter-data.pipe';

describe('FileterDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FileterDataPipe();
    expect(pipe).toBeTruthy();
  });
});
